"use strict";

const { NotFoundError } = require("../core/error.response");
const db = require("../models");

class UserRatingService {
  static async createRating(userId) {}
}

module.exports = UserRatingService;
